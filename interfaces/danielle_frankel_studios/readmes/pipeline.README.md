# Pipeline

Group: Daily Ops · File: `pipeline.tsx`

> See [`docs/CROSS_CUTTING.md`](../../../docs/CROSS_CUTTING.md) for rules shared across interfaces (Cobalt boundary, dark mode, sandboxing, etc.).

## Business Objective

Give DFS staff a single working view of every bride as they move through the sales & production pipeline (lead through fulfillment), so staff can see stage, flags, and next actions at a glance without cross-referencing Acuity/Shopify manually. Functions as both a Kanban board and a sortable/filterable list, backed by a Universal Full Client Profile modal for deep editing.

## Inputs

- Client records from `DF Clients` (`tblLLUlDgJ4ktzF7c`), production base `appUC2NFAlURayLx9` / sandbox `app6Q4xMZ1ngJxiV8`
- Stage data synced via Cobalt from Acuity (appointments) and Shopify/ApparelMagic (orders)
- Stakeholders: Julia Collins (product owner, DFS); DFS front-of-house/production staff (daily users)

## Outputs

- Pipeline Board View (existing Kanban, unchanged by recent work) and Pipeline List View (new sortable/filterable table)
- Universal Full Client Profile (`FullProfileModal`) — read/write surface for client data across all stages
- No direct external write-backs; edits write to Airtable fields consumed downstream by other interfaces/automations

## Workflow

1. Staff land on Pipeline and can toggle between Board and List View — both share the exact same underlying filtered client set, so switching never silently changes what's being viewed.
2. List View defaults to sorting by **Wedding Date** (not last-modified), matching how staff naturally prioritize near-term brides. It supports Studio, Sales Associate, Timeline, and Stage filters — the Stage filter is scoped to List View only, since Board View's columns already function as a stage filter (avoids a redundant control). All these filters persist in memory for the duration of the tab session (not auto-cleared on view switch).
3. Search results render as a dropdown rather than live-filtering the full page (keeps things responsive with a large client base). Search covers name, phone, email, and AM Order Number (Apparel Magic order identifier — the field that satisfies the "search by production order" requirement).
4. Clicking a client opens `FullProfileModal` (Universal Profile) — either "Current Stage" view (default) or "All Stages" view (toggle button, labeled "All Stages" / "Current Stage", no caret icon, styled as a filled primary button distinct from the plain "Go back" button).
5. Staff edit fields inline; edits are permitted for the current stage and all **past** stages (fully, functionally editable in both view modes) — this lets staff correct earlier-stage data without moving a client backward through the pipeline. Only strictly **future** stages are read-only.
6. Interest checkboxes (Alterations / Made-to-Measure) live in an always-visible "Interests" card and are directly editable regardless of stage or view mode — separate from the gated Deliberating-section copies of the same fields.
7. If `client.stage` doesn't match any value in `STAGE_ORDER`, the default view shows an explicit warning banner instead of rendering blank; header/interests/notes/flags still render normally. (All-Stages view already forces read-only in this case, so there's no conflict between the two behaviors.)
8. On a failed field save, the field reverts to its last saved value, surfaces a "Save failed" message, and allows retry — closing the modal returns to Pipeline with all filters and view mode intact.

## Rules

- `STAGE_ORDER` / `STAGE_STEPS` must be defined locally in this file — Interface Extensions run isolated; no shared imports across files (platform constraint, not fixable — the in-app "Edit code" mode is the only edit path). `pipeline.tsx` and `alterations.tsx` both duplicate this array identically. Mitigation: inline comments pointing to `docs/PHASE_LOGIC.md`, the single source of truth for all 7 pipeline phases.
- Past-stage editability: `isFutureStage` flags only strictly-future stages as read-only.
- `isFieldReadOnlyBySource` is an **independent** gate based on data source (Acuity/Shopify/AM sync) — not stage-based; fields sourced this way stay read-only for **all** users regardless of stage, since they're owned by the upstream integration, not manual entry. Do not conflate with stage read-only logic.
- Dark mode canonical pattern: `fulfillment.tsx`'s `useTheme()` hook (toggles a `dark` class on `<html>` based on `prefers-color-scheme`) + Tailwind `dark:` utility classes — chosen over `sample_tracker.tsx`'s bespoke inline-style token approach for direct compatibility with Pipeline's existing className-based styling. Applied across `DetailRow`, `FieldLabel`, `Editable*`, `BooleanDropdown`, popups, `StylePicker`, `MeasurementInputs`, and all cards/badges/flags.
- `BooleanDropdown` failed-save handling: reverts `localValue` to last confirmed value, surfaces "Save failed" via `FieldLabel`, allows retry (matches `EditableText`/`EditableNumber` pattern).
- Access: any user with Editor or Viewer access to the interface. Editors can edit all permitted fields per the stage-based rules above; Viewers can browse/filter/search/open the profile modal, but edits are gated by the interface's own read-only handling for that permission level.
- Never include `import './style.css';` in this file (standing rule across all interface files).
- Fixed 2026-07-29: `CLIENT_PICKED_PERCENT` was pointing at the wrong field ("% Picked" showed a value different from what the Order Ready automation actually evaluates — see `fulfillment.README.md`). Corrected to the real Orders rollup field, and the field is now always read-only in this file (was `EditableNumber`, a rollup can't be hand-edited).
- 2026-08-21 (Order Ready section, Issues 3 & 4 of the "Pipeline — Order Ready, Alterations & Fulfillment" batch): row order is now Items Sold / (Order Ready, Alterations, Client Notified — one `FieldRow`, equal width) / Customization Notes / an inline Orders table.
  - **Issue 3** — `Ship` (`fldQjLmwDokAkYPEt`) and `Pick Up` (`fldwqYAsQ3Iasi8QT`) were manual bride-level yes/no checkboxes that couldn't represent more than one order per bride, or reflect real per-order completion. They're no longer rendered here (still fetched into `usedFields`/`ClientData` — harmless, just unused — left alone rather than risk breaking another reader). Replaced with an inline table of the client's linked `Orders - Shopify` records (via the `shopify_order` link field, `fldWSGqQW9czYdams`), one row per order: Order # (`fldWiKEXjId411DQc`), Method (`delivery_method`, `fldFATO0oJUQjPEzr`), and Fulfillment Progress — a number + bar (`ProgressBar`, added to this file, same rendering as `fulfillment.tsx`'s own copy) driven by `fulfillment_progress_percentage` (`fldKDT2x7wmZ2Suui`, a 0–1 formula field on Orders). `client.shopifyOrderLinkIds` (raw linked record IDs, not the display-string `shopifyOrderNumber`) is what the table filters `orderRecords` against.
  - **Issue 4** — `Client Notified` in this section is now always read-only (regardless of the section's editable/read-only mode) and rendered as the same `ProgressBar`, driven by a rollup field `client_notified_percentage` (`fldKSGxe3uMdjHWz5`, live in both Sandbox and Production — confirmed via schema on 2026-08-21): rolls up `Orders.client_notified` across `shopify_order` with formula `IF(COUNT(values), SUM(values)/COUNT(values))`, i.e. the fraction of the bride's orders where the client has been notified. The old editable checkbox on this same underlying field (`client_notified_fulfillment`, `fldxumxeRnrDQ3CIk`) has since been replaced too — see Issue 6 below.
- Fixed 2026-08-21 (contributed by Alonso): RTW Size had the same shape of bug as the `CLIENT_PICKED_PERCENT` issue above — the modal's editable RTW Size field wrote directly to the customer's Acuity self-reported size (`fldvV2CiEx4RQN4mO`, "Size from Acuity Intake"), so any SA correction silently overwrote the original source data instead of recording a separate confirmed value. `DF Clients` has three related fields: the raw Acuity value (reference-only, never written to), a manual SA-confirmed value (`fldEEH4CK3Qqp0g0C`, the only one this modal writes), and a formula field (`fldSwfR25uvynWKI5`) that shows the manual value when set and falls back to the Acuity value otherwise. The fix: the editable view now writes to the manual field only, with the Acuity value shown alongside the label for reference (e.g. "RTW Size (0–20) | from Acuity: 8", via the new `rtwSizeLabelWithAcuity` helper); the read-only view now reads the fallback formula field instead of the manual field alone, so it shows the Acuity value rather than going blank before a size has been confirmed. The same Acuity/manual/display three-field pattern exists elsewhere in the base (e.g. Wedding Date) — read-only surfaces should always read the fallback/display field, never the raw source-only field, or they'll render blank whenever the manual/primary value hasn't been set yet.
- 2026-08-21 (In Alterations section, Issue 5 of the "Pipeline — Order Ready, Alterations & Fulfillment" batch): row order is now Last Alterations Appointment / Next Appointment / Alterations Appointments Held (`FieldRow`), then Sales Associate / Alterations Lead / Alterations In House / Alterations Status (`FieldRow4`), then the unchanged Items Sold / Total Spend / Alteration Notes.
  - **Timezone fix**: `formatAppointmentDateTime()` used to format with no explicit `timeZone`, so `toLocaleDateString`/`toLocaleTimeString` silently fell back to the *viewer's* browser/OS zone — a 1:30pm EDT appointment could render as 11:30am to someone viewing from Mountain time. Added `getStudioTimeZone(client.studio)` (`'LA'` → `America/Los_Angeles`, everything else — NY/Virtual/Mixed/unknown — → `America/New_York`) and now pass it explicitly at every call site across the file (Pipeline list view, Pre-Appointment, Deliberating, In Alterations), not just this section, since the underlying bug was the same everywhere `formatAppointmentDateTime` was used.
  - **Alterations Lead** (`next_appointment_alterations_lead`, `flddN7YHMuymJKbv9`) is now always shown via `DetailRow`, even blank (falls back to `—`) — it used to be omitted from this section entirely.
  - **Alterations In House** (`fldNjcDXIaGPGY1E6`) stays a plain checkbox on the backend (also read by `order_close_out.js` / the stage formula) but is now a dedicated `AlterationsInHouseToggle` button in the UI — small pill, "No Alterations" / "Alterations Needed" — never a checkbox or dropdown.
  - **Alterations Status** — new field (`fldtQfxqmecuIMXKY`, singleSelect; live in both Sandbox and Production as of 2026-08-21, now with two choices — "Pending" and "Done" — Julia's full status list may still grow beyond these two). Shown/editable only when `alterationsInHouse` or `hasAlterationsItem` (`has_alterations_item` / `shopify_alteration_order`, `fldWaqPw2BO4XQIbX`) is `TRUE`; otherwise renders `—`. Its options **and colors** are read live from the field's own Airtable schema each render (`alterationsStatusChoices`/`alterationsStatusOptions`/`alterationsStatusColors` in the main component, threaded into `FullProfileModal` as props) via the exact same pattern already used for the stage pill (`stageChoices`) — add/rename/recolor a choice directly in Airtable and it shows up here with no code change. `EditableSelect` gained an optional `colors` prop for this: when passed, both the closed box and every dropdown row keep the *same box shape as every other dropdown in this file* (e.g. `BooleanDropdown`'s Interest in Custom/Alts/M2M) but tinted with the choice's real Airtable color as the fill, instead of plain text on white.
- 2026-08-21 (In Fulfillment section, Issue 6 of the "Pipeline — Order Ready, Alterations & Fulfillment" batch): `Fulfillment Method`, `Client Notified`, `Tracking #`, and `3PL` were bride-level fields really tracking a per-order attribute — same fix direction as Issues 3/4. Removed all four from the bride-level layout (still fetched/derived, just unused — same "leave it, don't risk breaking another reader" call as Issues 3/4) and replaced with an inline `Orders - Shopify` table: **Shopify Order** (`fldWiKEXjId411DQc`, read-only, narrow fixed-width column via `<colgroup>`), **Fulfillment Method** (`fulfillment_method`, `fldNX1vEzvndF6ozO` — deliberately just its native "Pick Up"/"Shipping" choices, not the bride-level field's 5 location-specific ones; per Axel, no added choices for now), **Client Notified** (`client_notified`, `fldve9YvP16XtrHN2`), **Tracking #** (`tracking_number`, `fldCfwwMFNkVKJApj`, read-only), **3PL** (`third_party_logistics`, `fld4jzdVnIQ7JzU7U` — *not* the similarly-shaped `carrier` field, `fld3JafhFWzW6Knuw`, which was wired up first and then corrected; read-only), and **Fulfillment Progress** (`fulfillment_progress_percentage`, `fldKDT2x7wmZ2Suui`, same `ProgressBar` as the Order Ready table). Only Fulfillment Method and Client Notified are editable — Shopify Order/Tracking #/3PL stay read-only, same as they always were. New table-cell components `OrderEditableText`/`OrderEditableCheckbox`/`OrderEditableSelect` write against a specific Orders record instead of the client record (no `FieldLabel` chrome — the column header already names the field). The old hardcoded "Tax + Shipping" text (`taxShippingDisplay`) was dropped along with bride-level Fulfillment Method, since neither applies once this is per-order; `Address Confirmed` and `Do Not Ship Until` (not part of this issue) moved up into the row that used to hold `% Picked`/Fulfillment Method/Tax + Shipping.
  - **Bug fixed in the same pass:** the table initially used `getOrderSel()` (which expects a `{name}` select-shaped cell value) for `Tracking #`, a plain text field — `v?.name` on a string is always `undefined`, so it silently rendered `—` for every tracking number. Fixed by reading it via `getCellValueAsString` instead. Any per-order text field added to this table in the future needs the same treatment, not `getOrderSel`.
  - **`fulfillment_progress_percentage` formula corrected (2026-08-21, live in Sandbox and Production):** was `SUM(picked_status_percentage, shipped_status_percentage)` — double-counting, since `picked` means "ready to hand off" (allocated in Apparel Magic) and `shipped`/`picked up` means "already handed off to the client," not two halves of one total. Verified in Production (`order_items`): `quantity_shipped = 1` always implies `quantity_open = 0` (0 counterexamples), but the reverse doesn't hold — 105+ records have `quantity_open = 0` with `quantity_shipped = 0` (quantity_open is itself an independent AM-synced signal, not simply `quantity − picked − shipped`), so `quantity_shipped` alone would undercount. Added `order_items.quantity_closed_flag` (`fldb3T0Z7PvhMXKHQ`, formula, `1` if that item's `quantity_open = 0` else blank) and `Orders - Shopify.quantity_closed_count` (`fldhoS71zL4C9aJ4W`, rollup `SUM(values)` of the flag across the order's items) — both live in Sandbox and Production. Axel's replacement formula (`ROUND(IF(quantity, quantity_closed_count/quantity), 2)`, using the pre-existing quantity-sum rollup as the denominator rather than an item-count field) is now live in both environments. A separate `order_items_count` field (`fldrUuiZqm61cVAp0`, count of linked `order_items`) was also created in Sandbox as an alternative denominator but ended up unused — exists only in Sandbox, not referenced by the live formula, safe to ignore or delete.
  - `fulfillment_progress_percentage` is also read by `fulfillment.tsx` (Pickup Readiness Gate) — this formula fix changes the *meaning* of that number everywhere it's used, not just in this file.
- **Wedding Date filter, added 2026-08-23** (replicates `alterations.tsx`'s existing exact-date filter — same field, same comparison rule, not a new pattern): `WeddingDateFilterButton`, an icon-only round capsule (calendar glyph + a small hand-drawn `RingIcon` badge in the corner, since Phosphor Icons has no wedding-ring icon) placed right after the Timeline filter — deliberately icon-only rather than another labeled dropdown, since the filter row was already 4-5 controls wide and a text pill would push it into wrapping sooner than needed. Filters `filteredClients` directly, so it narrows **both** Board and List View from the same underlying set (List View is the only one that visually surfaces Wedding Date as a column, but Board View's card counts still reflect the filter). Reads `Wedding Date (Formatted)` (same field as the List View sort, not the fallback display field — an exact-date filter needs a real comparable date). Compares dates as raw `"YYYY-MM-DD"`-prefixed strings (`c.weddingDate.slice(0, 10)`), never round-tripped through `new Date()` — an earlier version compared via `parseDateFlexible(...)` + `new Date(isoString)`, which parses a date-only ISO string as UTC midnight and reads a day earlier for any viewer west of UTC (same bug class already fixed for Wedding Date elsewhere in this codebase). The popup itself is `CalendarPopup` (already existed in this file for another date field) — extended with an optional `footerLabel` prop (shown above the month header, not below the grid) so the icon-only trigger's meaning is still unambiguous once open; every other existing `CalendarPopup` caller omits it and is unaffected. `FixedPopup` (the shared popup-positioning helper) gained horizontal viewport clamping in the same pass — a trigger near the right edge of the screen used to produce an off-screen, unusable popup; this fix benefits every `FixedPopup` caller in the file, not just this one. Filter row container is now `flex-wrap` so a narrow viewport reflows controls to a second line instead of silently overflowing — no individual control shrinks or compresses.
