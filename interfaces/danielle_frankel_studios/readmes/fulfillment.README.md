# Fulfillment

Group: Daily Ops · File: `fulfillment.tsx`

> See [`docs/CROSS_CUTTING.md`](../../../docs/CROSS_CUTTING.md) for rules shared across interfaces (Cobalt boundary, dark mode, sandboxing, etc.).

## Business Objective

Track orders through fulfillment: pricing change transparency, hold/shipping status, and pickup readiness — so staff can see exactly what changed on an order and whether it's cleared to ship/pick up.

## Inputs

- Orders - Shopify (`tblHFGbijtvZcRPkE`) records, including snapshot fields
- `order_sync_changelog` (`tblOCgG5WDP51FB2n`) — auto-logged by automations; separate from the existing manual `order_adjustments` table (which staff create by hand in the interface — do not conflate the two)
- Cobalt-synced Shopify/ApparelMagic price data; `order_items` table (Cobalt-owned, polling sync — see the Pick Ticket item in Rules)
- Stakeholders: Julia Collins; Cobalt (Nadiia/Cassidy) for sync-level questions (change reason / prior field value in the Shopify webhook — questions sent, still pending response as of last check)

## Outputs

- `OrderDetailModal`: read-only Sync Change Log section (see [Change Log](../../../docs/CROSS_CUTTING.md#interfaces-without-custom-code-in-this-repo))
- Hold reason + "On hold until [date]" display
- Three independent RYG (Red/Yellow/Green) Pickup Readiness flags: Tax, Address, Hold — deliberately not one combined "Readiness Checklist Passed" field, so staff can see exactly which check is unresolved
- `pickup_released`: always a manual staff checkbox, never auto-derived, so a later change to underlying checks can't silently un-release an order that already shipped
- Three automations (OOP pattern): init snapshot on create; compare-and-log on update; one-time backfill (already run, now deleted)

## Workflow

1. Order created in Shopify → synced to Orders - Shopify → init-snapshot automation fires, capturing baseline price fields.
2. On any Shopify/AM price update while the order is `In Fulfillment`, the compare-and-log automation diffs against the snapshot and writes a row to `order_sync_changelog`.
3. Staff view the read-only Sync Change Log in `OrderDetailModal`.
4. Staff see hold reason + hold-until date if the order is on hold.
5. Pickup Readiness Gate shows the three independent RYG flags across four surfaces consistently: master client table, Client Detail Modal title bar, Shopify Orders sub-table, and Order Detail Modal. Severity model: green = passed; yellow = failed with zero fulfillment progress; red = failed with progress already underway; gray = underlying data couldn't be resolved.
6. Item-level fulfillment status uses `quantity_open === 0` as the source of truth for "Fulfilled" (not a sum of `quantity_picked + quantity_shipped`), since `quantity_open` is the more direct signal already maintained by the ApparelMagic sync.
7. Save operations for `tax_confirmed` and `pickup_released` roll back the optimistic UI state and surface an inline error if the Airtable write fails — staff should never see a release reflected in the UI that didn't actually persist.
8. Staff manually toggle `pickup_released` once all checks pass and they've physically confirmed release.

## Rules

- `CONFIG.REQUIRED_STAGE` for the compare-and-log automation must be exactly `'In Fulfillment'` — not `'Fulfillment'`.
- `hold_shipment_date` is a manually-created lookup field (`fldS8LJdNHBKJhC0`) — Sandboxing on this base (`app6Q4xMZ1ngJxiV8`) blocks API field creation, so any new fields must be created manually in Airtable, never via automation/API.
- No dedicated "hold reason" field exists yet at client or order level — `fld2MAllXcFTSIOVZ` is a lookup, not a true editable hold-reason field. Open gap, not yet resolved.
- Cobalt questions about change reason / prior field value in the webhook payload are pending (Nadiia/Cassidy) — do not assume this data is available until confirmed; the questions sent were specifically: (1) whether Shopify sync includes any reason/event/metadata for a price-field change, and (2) whether the previous value is available at sync time or needs to be captured in Airtable right before it's overwritten.
- `tax_confirmed` is a **permanent lock** once set true — added during implementation, not in the original AC; pending Julia's formal confirmation. This exists to prevent accidental un-confirmation after downstream release decisions have already been made.
- Access: restricted to DFS Staff with existing Fulfillment page permissions — no new access group was introduced by the Pickup Readiness Gate work. Enforcement of who can see/edit is delegated to Airtable's own collaborator permissions, not implemented in interface code.
- Tax rate calculation and freight/shipping charge calculation logic are explicitly unchanged/out of scope for the Pickup Readiness Gate story — it only adds confirmation and gating around existing values; a future story is expected to source accurate state tax rates automatically.
- Order Ready threshold (feeds fulfillment eligibility upstream, built as its own automation on `DF Clients.stage`): gown item's `Category Lookup` (`fldSF1GXY5MgiAXdl` = `"GOWN"`) picked AND `picked_status_percentage` (`fldjC8M11Pis7eMxF`) > 75% — either condition qualifies, writing `stage = "Order Ready"`. The choice-field rename from the old internal value "In Production" has been published, so this is now consistent with `pipeline.tsx`'s `STAGE_ORDER`/`STAGE_STEPS`. Percent-formula fields return decimal fractions via the API (1.0 = 100%), so automations must compare against `1` with epsilon tolerance, never `100`.
- **Fixed 2026-08-21 (Wedding Date column, main table):** the master table's "Wedding Date" column read `Wedding Date (Formatted)` (`fldbgknumKGS5W5WU`) raw and formatted it with `new Date(val).toLocaleDateString(...)` — no timezone anchor, so a `YYYY-MM-DD` string got interpreted as UTC midnight and displayed one day earlier in any negative-UTC-offset browser (e.g. a wedding on 10/10/2026 showed as 10/9/2026). It also never fell back to `Wedding Date (If Not Set)` (manual placeholder text) when Formatted was empty. Repointed this one column to the read-only formula field `wedding_date_display` (`fldfDHXcCEbFHEX4a`, already resolves the Formatted/If-Not-Set fallback at the data layer) and added `formatWeddingDateDisplay()`, which anchors `YYYY-MM-DD` strings to `T12:00:00Z` and formats with `timeZone: 'UTC'` to avoid the day-shift, renders as `Mmm dd, YYYY` (e.g. `Oct 10, 2026`, per Alonso 2026-08-21 — a brief `MM/DD/YYYY` numeric attempt was reverted the same day since this column's original format was correct), and returns any non-date text (e.g. `"Spring 2027"`) as-is instead of blanking it. Scope was deliberately limited to this one column — the editable `DatePicker` in the client detail modal (still correctly bound to `Wedding Date (Formatted)`, since that's the field staff actually edit here) and the wedding-date sort comparator (only used for ordering, not display, so the UTC shift doesn't affect relative order) were both left untouched.
- Fixed 2026-07-29: the `PICKED` field constant in this file (labeled "% Picked" on the client/order surfaces) was pointing at the wrong field entirely — a field distinct from `picked_status_percentage` above, and not the one the Order Ready automation actually evaluates. Corrected to the real Orders rollup field. The two "picked" fields are confirmed **not** interchangeable — don't assume they track the same underlying data.
- Known open bug (Julia, Jul 15, High priority): the "Order Ready" filter shows no results even though the implemented logic matches Julia's written rule — this is an **execution bug**, not a logic error; the rule itself (gown picked OR >75%) is correct per her most recent written confirmation, so don't "fix" the threshold itself, fix the filter execution.
- This page will need to incorporate Pick Ticket backend updates from Cobalt (`order_items` table: allocated/picked/shipped quantity + percentage formula fields, polled every 10 min for recent orders and daily at 4 AM for the last 6 months) — not yet scoped as a story. Open scoping question: does `picked_status_percentage` from `order_items` replace or complement the existing Order Ready threshold logic above? Known Cobalt-side data risk: some `order_items` records lack linked style records due to non-standardized style names between Shopify and ApparelMagic — this is a Cobalt data-quality issue, not a Singular edge case to design around.
- Cobalt owns Acuity/Shopify/AM pipelines and `Error Logs` — never write to Cobalt-managed tables from this interface.
- Fulfillment had 3 conflicting deployed phase-transition automations found during the 7-phase audit; "No Alts/Order Ready - Update Phase to In Fulfillment" was confirmed as champion and the other two were deleted — if you find more than one Fulfillment-stage automation in the base, treat this as a red flag and confirm which is authoritative before assuming both are intentional.
- Fixed 2026-07-22: `fulfillmentRecords` (the queue-membership filter) previously required, in addition to `Stage === 'In Fulfillment'`, a non-empty `Items Sold` AND a `Wedding Date` today-or-later — none of which is part of the phase-logic definition ("Clients who are in Order Ready and do not have Alterations as an order item" per `slack-files/recovered/AIRTABLE PHASE LOGIC.docx`, "Fulfillment" row). Since most `In Fulfillment` clients don't have those lookup fields populated on the `DF Clients` record, the extra conditions were silently dropping ~80% of legitimately-staged clients (only 3 of 27 were passing). The gate now checks `Stage` only. See `automations/danielle_frankel_studios/fulfillment_backfill.js` for the one-time backfill that advances clients who already qualified for `In Fulfillment` (per the same phase-logic rule) but never got the automation-driven stage update.
- **In Fulfillment close-out (2026-08-20, in progress):** `DF Clients.stage` (`fldLcxVZvI1rigBlh`) has a terminal choice after `"In Fulfillment"` — originally named `"Picked Up"` (`sel9gJfBcN2v0VLTc`), **renamed to `"Fulfilled"`** on 2026-08-20. Live audit before renaming found two already-**deployed** automations writing this select field directly (not built as scripts — condition-based "record matches conditions" + "update record" nodes, so they weren't visible in this README's automation list before): `"Picked Up & Full Order Fulfilled - Update Phase to Picked Up"` (fires when an appointment record's `Full Order Fulfilled` + `Picked Up` checkboxes are both true) and `"Order is Shipped"` (fires when `tracking_number` is not empty AND `% Shipped` = 100%, writing the separate `"Shipped"` choice, `selqqanLc3ZBBouGj`). Both had **zero runs in production** (confirmed via each automation's Run History) despite being toggled on, and 702 clients already had `"Picked Up"` vs. 0 with `"Shipped"` — so the `"Picked Up"` value on those 702 came from somewhere other than these two automations (manual entry/import, not yet traced). Given the zero-run history, both were safe to repoint: `"Order is Shipped"` (renamed to `"...Update Phase to Fulfilled"`) now writes the same `"Fulfilled"` choice instead of `"Shipped"`, and the `"Shipped"` choice was deleted from the field entirely. `"Picked Up & Full Order Fulfilled..."` (renamed to `"...Update Phase to Fulfilled"`) needed no logic change — it already wrote the same choice id that now displays as `"Fulfilled"`. Rationale for unifying: `stage` is a single client-level value, but a client can have multiple orders each individually picked up or shipped, so close-out converges to one combined `"Fulfilled"` rather than tracking per-order method.
- **Close-out design (v3.0.0, 2026-08-20 — final for this batch):** two independent paths, either qualifies, both write `stage = "Fulfilled"`: (A) `quantity_open_total == 0` on DF Clients — a chained rollup: `order_items.quantity_open` → `Orders - Shopify.quantity_open_total` (rollup, `SUM(values)`, with an "only include linked records that meet conditions" filter — `style_category has none of ALTERATIONS`, added directly on this rollup by Axel; this is the Issue 2 fix, baked in at the source) → `DF Clients.quantity_open_total` (`fldVfOcvePRxXkVHT`, plain `SUM(values)` rollup); (D) manual override checkbox `sa_override_fulfilled` (`fldqHU9ryVXgpZOGe`, snake_case per base convention) — a true override, doesn't require A. **Paths B (appointment-confirmed pickup: `full_order_fulfilled_from_appt` + `picked_up_from_appt`) and C (`tracking_number` + `percent_shipped == 100%`) were dropped entirely** after Axel reviewed field dependencies: B's two lookup fields have no other meaningful dependents worth building close-out logic on, and C's `percent_shipped` rollup chains through an Orders - Shopify field literally named `"Shipped (num, 0-1) (deprecated)"`, itself derived from a manually-editable single select (`"Shipped status"`) — shipped quantity should come from `order_items` (Apparel-Magic-synced) instead, so path C was trusting the wrong source of truth. Axel is following up with Julia on whether to delete `full_order_fulfilled_from_appt`, `picked_up_from_appt`, `percent_shipped`, and the two Orders - Shopify fields behind it — no schema deletions made yet, the close-out logic just stopped referencing them. AM-sourced shipping-label status was considered even earlier and dropped the same way — Cobalt doesn't expose it. `pipeline.tsx`/`alterations.tsx` `STAGE_ORDER` and `order_ready_evaluation.js`'s guard array include `"Fulfilled"` (not `"Shipped"`, since that choice was deleted) so the "never regress a stage" guards stay correct; Pipeline's Kanban renders a separate `KANBAN_STAGE_ORDER` (excludes `"Fulfilled"`) so closed orders disappear from the board while still reachable from the List view's Stage filter.
- **View-based trigger:** per Axel's feedback that `recordUpdated` re-fires on every intermediate field change even when the result still doesn't qualify, the close-out automation's trigger moved to **"record enters view"** on a new view, **`fulfillment_ready_to_close_out`** (DF Clients), filtered to `stage = "In Fulfillment" AND (quantity_open_total = 0 OR sa_override_fulfilled)` — built by hand in the Airtable UI, since nested OR-of-AND condition trees aren't buildable via the MCP (see below). This view is also directly useful on its own as a visual queue of what's about to close out.
- **`recordMatchesConditions` via the API — confirmed unsupported for nested logic:** tried building a `recordMatchesConditions` trigger via the MCP twice (a top-level AND with a nested OR-of-ANDs, then a flattened OR of 4 fully-expanded ANDs) — both rejected: the MCP's `recordMatchesConditions` trigger schema only accepts a **flat list of leaf conditions** under one `and`/`or`, not nested groups. The Airtable UI's own condition builder (both for automation triggers and view filters) does support nested groups (`"Add condition group"`) — which is exactly how the `fulfillment_ready_to_close_out` view filter above was built by hand. Worth remembering for future automation work: any condition tree deeper than one level needs the UI, not the API.
- **Automation history:** three legacy automations were deleted by Axel once `order_close_out.js` was verified — the original consolidation draft (`wflndcP1aaQD2ORhK`, which had gotten a real `customScript` node pasted in before the `quantity_open_total` rollup existed, locking it from further API edits), `"Picked Up & Full Order Fulfilled - Update Phase to Fulfilled"` (`wflfzaGLUMgFYZUub`), and `"Order is Shipped - Update Phase to Fulfilled"` (`wflyByHV0Xxkn1J0C`) — all three had zero production runs, so nothing in-flight was lost. The automation now live is **`Order Close Out v2 - In Fulfillment to Fulfilled`** (`wfl2t4lgiPEzMLa5w`), retargeted by Axel to the view-based trigger above. **TODO (Axel):** paste `order_close_out.js` (v3.0.0) into its script node if not already done.
- **Sandbox test records:** Bryn Valaika (`rec2BI4tH8l9hfKYn`) and Elizabeth Lites (`rec9cYK6eYq7PRDGu`) tested Path A and D respectively — both passed. Anna Davis and Josephine D'Ippolito (originally set up for the now-dropped Paths B/C) are no longer relevant test cases. Additional fresh records for retesting v3: **Path A** — Sidney Jaet (`rec4j8YjbKuQ3lTV2`) and Krystle Ongjanco (`rec6R05zT1hrhkKXM`), both already at `quantity_open_total = 0` in "In Fulfillment," no edits needed. **Path D** — Anna Davis (`rec055XF3Qlo97p7e`, `quantity_open_total = 3`, so the override checkbox is the only thing that would qualify her — good isolation for testing D specifically).
- **`percent_shipped` usage check (2026-08-20):** grepped `pipeline.tsx` for `percent_shipped` / `fldigqrFBZwceLCT7` — zero hits. It's not read or computed by any custom logic there. It only shows up in an Interfaces page config (a grid block's allowed-fields list and column `visibility: true`) — i.e. it's exposed as a visible column somewhere in Interfaces, not wired into any functional code. Supports Axel's plan to review with Julia whether to keep or delete it (and the two Orders - Shopify fields behind it) — deleting it would be a display-only change on that one interface block, not a code change.
- **Test records (sandbox, 2026-08-20):** four "In Fulfillment" clients set up for manual QA before turning v2 on — none of paths B/C/D occur naturally today (0 records base-wide satisfy either combo, confirming they never ran in production):
  - Path A (quantity_open_total already 0, no edits needed): **Bryn Valaika** (`rec2BI4tH8l9hfKYn`) and **Elizabeth Lites** (`rec9cYK6eYq7PRDGu`) both already qualify live.
  - Path D (manual override): use **Elizabeth Lites** (`rec9cYK6eYq7PRDGu`) — check her `sa_override_fulfilled` box directly (it's a real checkbox on DF Clients).
  - Path B (appointment pickup): use **Anna Davis** (`rec055XF3Qlo97p7e`) — `full_order_fulfilled_from_appt`/`picked_up_from_appt` are lookups (not directly editable), sourced from her linked Appointment record via the `fldYb8G67izm3qelZ` link field; check "Full Order Fulfilled" (`fldFZTWnXjWO4lwH2`) and "Picked Up" (`fldaT5YwVqB87h8Ia`) on that Appointment record instead.
  - Path C (tracking + 100% shipped): use **Josephine D'Ippolito** (`rec0oHZQ71VCXSJKG`) — set her `tracking_number` field directly (plain text, editable on DF Clients), and set the shipped-percent source field (`fldJTYc3h1qvYjh1I`) to 100% on one of her linked Orders - Shopify records (`percent_shipped` on DF Clients is a rollup, not directly editable).
- Base ID correction: this doc and several automations refer to the sandbox as `app6Q4xMZ1ngJxiV8`; live verification on 2026-08-20 found the actual sandbox base (matching all table/field IDs referenced here) is `appMmEE4zyHMGhkkd`. Treat `app6Q4xMZ1ngJxiV8` in older comments as stale.
